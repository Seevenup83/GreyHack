jumpServer = "29.88.203.47"; portJump = 99
endServer = "192.168.1.1"; portEnd = 22
shellJump = get_shell.connect_service(jumpServer, portJump, "root", "Umming", "ssh")
if typeof(shellJump) == "string" then exit(shellJump)
if shellJump then
	shellEnd = shellJump.connect_service(endServer, portEnd, "root", "Umming", "ssh")
    shellEnd.start_terminal
    //shellEnd.launch("/bin/rshell_interface")
else
    exit("connection failed")
end if
